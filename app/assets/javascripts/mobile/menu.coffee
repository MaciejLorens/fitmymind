$ ->

# ====== MENU ==========================================================================================================

  $(".menu").click (e) ->
    e.preventDefault()
    $(".menu").removeClass('active')
    $(@).addClass('active')
    target = $(@).data('target')
    $(".mobile-section").hide()
    $("#" + target).show()


#  ===== PRIVATE =======================================================================================================
